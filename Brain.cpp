#include "Neuron.cpp"
#include "Synapse.cpp"

class Brain {
private:
  int numNeurons;
  //initialize neuron+synapse arrays
public:
  Brain(_numNeurons) {
    numNeurons = _numNeurons;
    // Add neurons and synapses
  }
  void update() {
    //update neurons
  }
}
